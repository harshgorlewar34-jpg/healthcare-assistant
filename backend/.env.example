PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/medibot
JWT_SECRET=medibot_super_secure_jwt_secret_2026_key

# AI Provider Options: "gemini", "openai", or "rule-based" (default fallback if keys not provided)
AI_PROVIDER=gemini

# Provide your API key here (Frontend never sees this):
GEMINI_API_KEY=
OPENAI_API_KEY=
